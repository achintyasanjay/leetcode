class Solution:
    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:
        # create adjacency list
        # create in-degree list
        # create queue
        # add all nodes with 0 in-degree to the queue
        # while queue is not empty
        # pop from queue
        # add to result
        # for each neighbor, reduce in-degree by 1
        # if in-degree of neighbor is 0, add to queue
        # if result length is not equal to number of courses, return empty array
        return []